Parked: chain-wide activity via block sampling (`fetch_rh_activity.py`) and L1 batch-poster cost (`fetch_l1_cost.py`).
Both work but are estimates / rate-limit bound on free RPCs. Replaced by DeFiLlama (`fetch_llama.py`). Revive if we run our own Robinhood Chain node.
